 #include<iostream>
 #include<string>
 using namespace std;
 int main()
 {
 string str="college";
 string str1("wallah");
 cout<<str<<endl<<str1;
 getline(cin,str); // string input
//  indexing similar to array
// some in built function of string 
/*
reverse(start_ptr,end_ptr):
reverse(str.begin(),str.end()):
time complexity O(n);
substr(position,length) use to find substring means part of string
 str1+str2 plus operator used to concatenate string
 strcat(): used to concatenate two char array
 str.size(): to fing size or we can use str.length()
 work in O(1) time
 if character array is given then use strlen() work in O(n) time
 str.push_back(char): used to insert character at end of string
 to_string(num): used to convert int val to string 
  




 */
 
 return 0;
 }